# ============================================
#  ApiManager - Database Configuration
# ============================================
# Enter your MySQL connection details below.
# Make sure the database exists and the user has access.
# ============================================

database:
  host: "127.0.0.1"          # IP or hostname of your database server
  port: 3306                 # Default MySQL port
  name: "my_database"        # Name of the database (schema)
  username: "root"           # Database user
  password: "password123"    # User password